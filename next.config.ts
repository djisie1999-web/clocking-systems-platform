import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // v2: default output mode — `next start` serves all static assets correctly
  poweredByHeader: false,
};

export default nextConfig;
