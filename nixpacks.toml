[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm install --production=false"]

[phases.build]
cmds = ["npx prisma generate", "npm run build"]

[start]
cmd = "npx prisma db push --skip-generate && npx next start -p ${PORT:-8080}"
